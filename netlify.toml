# site deploy: Netlify keeps deploying from this repo, now scoped to apps/site.
[build]
  base = "apps/site"
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  # Vite 8 requires Node >= 22.12
  NODE_VERSION = "22"
